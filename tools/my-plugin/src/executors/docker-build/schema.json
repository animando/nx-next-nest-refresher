{
  "$schema": "https://json-schema.org/schema",
  "version": 2,
  "title": "DockerBuild executor",
  "description": "",
  "type": "object",
  "properties": {
    "targets": {
      "type": "string",
      "description": "Comma-separated list of targets to build (optional)"
    },
    "dockerFile": {
      "type": "string",
      "description": "The path to the docker file (optional, defaults to apps/<application>/Dockerfile)"
    }
  },
  "required": []
}
